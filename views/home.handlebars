<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <style>
        body {
            font-family: sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: #f5f5f5;
        }

        .message {
            font-size: 2em;
            text-align: center;
            color: #333;
        }

        .message span {
            color: #f08080;
        }
    </style>
</head>

<body>
    <h1 class="message">Welcome, <span>{{ name }}</span>!</h1>
</body>

</html>